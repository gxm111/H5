module junyou.hqg {
    export class SimpleItemSlot extends sui.Slot {
        // protected lockIcon: egret.Bitmap;
        //protected jobIcon: egret.Bitmap;
        //protected xianshiIcon: egret.Bitmap;

		/*lOffsetX: number;
		lOffsetY: number;

        public constructor() {
            super();
        }

		public bindComponent() {
			super.bindComponent();
            this.lockIcon = new egret.Bitmap();
        }

        protected setLock() {
            let lockVisible: Boolean = this.data && this.data.bound;
			if (lockVisible) {
				// this.lockIcon.bitmapData=SharedBMDHelper.boundBMD;
				this.skin.parent.addChild(this.lockIcon);
				this.lockIcon.x = this.scale9Grid.x;
				this.lockIcon.y = this.scale9Grid.y;
				this.lockIcon.visible = true;
			}
			else {
				this.lockIcon.visible = false;
			}
        }

        public awake() {
            super.awake();
            this.setLock();
        }*/
    }
}